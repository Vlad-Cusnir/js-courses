// debugger;

// DATA INPUT SECTION
let firstNr = prompt('Input the first number');
firstNr = Number(firstNr);

let secondNr = prompt('Input the second number');
secondNr = Number(secondNr);

// PROCESSING SECTION
let sum = firstNr + secondNr;

// DATA OUTPUT SECTION
alert(`Sum is: ${sum}!`);